server {
    listen {{ nginx_listen_port }} default_server;
    listen [::]:{{ nginx_listen_port }} default_server;

    server_name _;

    location / {
        default_type text/plain;
        return 200 "nginx on port {{ nginx_listen_port }}\n";
    }
}
